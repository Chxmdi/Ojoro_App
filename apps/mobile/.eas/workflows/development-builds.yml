name: Ojoro Development Builds

jobs:
  build_android:
    name: Android development build
    type: build
    params:
      platform: android
      profile: development

  build_ios:
    name: iOS development build
    type: build
    params:
      platform: ios
      profile: development
